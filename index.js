'use strict';

const buildEntityElement = require('./src/entity-builder');
const buildPage = require('./src/page-builder');

module.exports = {
  buildEntityElement: buildEntityElement,
  buildPage: buildPage
};
